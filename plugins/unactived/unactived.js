function initContainer() {
    var warning = document.createElement('div')
    warning.classList.add('dsp_spider_pluging')
    warning.innerHTML = '未激活'
    document.body.appendChild(warning)
}

initContainer()
